# This 'application.properties' file is the default
# for setting up different properties
# for the current spring boot application
# This is the set the current active profile for the project
spring.profiles.active=test
